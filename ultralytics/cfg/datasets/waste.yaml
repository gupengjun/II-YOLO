# 本项目下的dataset中的数据集仅供大家参考存放格式和调试代码是否能跑通
# dataset path
train: ../../../floatwaste2/train/images  # path + train
val: ../../../floatwaste2/val/images # path + val
test: ../../../floatwaste2/test/images # path + test

# number of classes
nc: 1

# class names
names: ['waste']